import { ClassificacaoPipe } from './classificacao-pipe';

describe('ClassificacaoPipe', () => {
  it('create an instance', () => {
    const pipe = new ClassificacaoPipe();
    expect(pipe).toBeTruthy();
  });
});
